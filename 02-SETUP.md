# 02 — Setup desde cero

## Pre-requisitos

```bash
node --version        # >= 22.0.0
corepack enable
corepack prepare pnpm@9.15.4 --activate
pnpm --version        # 9.15.4
```

## Paso 1: Crear el proyecto NestJS

```bash
cd ~/Desktop
npm i -g @nestjs/cli
nest new lab-back --package-manager pnpm
cd lab-back
```

Cuando te pregunte, elegí **pnpm** como gestor.

## Paso 2: Configurar TypeScript

Editar `tsconfig.json`:

```json
{
  "compilerOptions": {
    "module": "commonjs",
    "declaration": true,
    "removeComments": true,
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    "allowSyntheticDefaultImports": true,
    "target": "ES2022",
    "sourceMap": true,
    "outDir": "./dist",
    "baseUrl": "./",
    "incremental": true,
    "skipLibCheck": true,
    "strictNullChecks": true,
    "noImplicitAny": true,
    "strictBindCallApply": true,
    "forceConsistentCasingInFileNames": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
```

## Paso 3: Instalar dependencias adicionales

```bash
# Producción
pnpm add @nestjs/config @nestjs/swagger \
         @supabase/supabase-js@2.47.10 \
         @react-pdf/renderer@4.1.6 \
         class-validator class-transformer \
         drizzle-orm@0.45.2 postgres@3.4.5 \
         decimal.js@10.4.3 react@19.0.0 \
         helmet compression

# Dev
pnpm add -D @biomejs/biome@1.9.4 \
            drizzle-kit@0.31.10 \
            @types/react@19.0.2 \
            rimraf
```

## Paso 4: Estructura de carpetas inicial

```bash
mkdir -p src/{modules,db,common,auth,domain,pdf}
mkdir -p src/db/{schema,migrations}
mkdir -p src/common/{guards,decorators,filters,interceptors,pipes}
mkdir -p src/domain/{money,pricing,status,validation}
mkdir -p src/pdf/templates
mkdir -p test
```

Resultado esperado:

```
lab-back/
├── src/
│   ├── modules/         # Un módulo por dominio (patients, orders, etc.)
│   ├── db/
│   │   ├── schema/      # Drizzle table definitions
│   │   └── migrations/  # SQL migrations generated by drizzle-kit
│   ├── common/
│   │   ├── guards/      # AuthGuard, RolesGuard
│   │   ├── decorators/  # @CurrentUser(), @Roles()
│   │   ├── filters/     # ExceptionFilter global
│   │   ├── interceptors/ # Logging, transform
│   │   └── pipes/       # ValidationPipe global
│   ├── auth/            # Supabase JWT verification
│   ├── domain/
│   │   ├── money/       # decimal.js helpers
│   │   ├── pricing/     # NBU engine
│   │   ├── status/      # Order status FSM
│   │   └── validation/  # Reference range classification
│   ├── pdf/             # PDF render + templates
│   ├── app.module.ts    # Root module
│   ├── main.ts          # Entry point
│   └── config.ts        # env loader
└── test/
```

## Paso 5: Configuración de Drizzle

Crear `drizzle.config.ts` en la raíz:

```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema/*.ts',
  out: './src/db/migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
  verbose: true,
  strict: true,
});
```

Scripts en `package.json`:

```json
{
  "scripts": {
    "build": "rimraf dist && nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "jest",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  }
}
```

## Paso 6: Bootstrap mínimo

Reemplazar `src/main.ts` con:

```typescript
import { NestFactory } from '@nestjs/core';
import { ValidationPipe } from '@nestjs/common';
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import helmet from 'helmet';
import compression from 'compression';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);

  // Security
  app.use(helmet());
  app.use(compression());

  // CORS
  app.enableCors({
    origin: process.env.CORS_ALLOWED_ORIGINS?.split(',') ?? ['http://localhost:3000'],
    credentials: true,
    methods: ['GET', 'POST', 'PATCH', 'DELETE', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization'],
  });

  // Global pipes (validation)
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,
      forbidNonWhitelisted: true,
      transform: true,
      transformOptions: { enableImplicitConversion: true },
    }),
  );

  // Global prefix
  app.setGlobalPrefix('api');

  // Swagger / OpenAPI
  const config = new DocumentBuilder()
    .setTitle('Lab Bioquímico API')
    .setDescription('REST API del sistema de laboratorio')
    .setVersion('1.0')
    .addBearerAuth()
    .build();
  const document = SwaggerModule.createDocument(app, config);
  SwaggerModule.setup('api/docs', app, document);

  const port = Number(process.env.PORT) || 4000;
  await app.listen(port);
  console.log(`lab-back listening on http://localhost:${port}`);
}

bootstrap();
```

## Paso 7: App Module inicial

`src/app.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';

@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      envFilePath: '.env',
    }),
    // Los módulos de dominio se agregan acá (ver 05-MODULES.md)
  ],
})
export class AppModule {}
```

## Paso 8: Health check inicial

Crear `src/modules/health/health.controller.ts`:

```typescript
import { Controller, Get } from '@nestjs/common';

@Controller()
export class HealthController {
  @Get('healthz')
  health() {
    return { ok: true, time: new Date().toISOString() };
  }
}
```

Y `src/modules/health/health.module.ts`:

```typescript
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({ controllers: [HealthController] })
export class HealthModule {}
```

Importarlo en `AppModule`.

## Paso 9: Verificación

```bash
pnpm start:dev
```

Debería levantar en `http://localhost:4000`.

Probá:
- `http://localhost:4000/api/healthz` → `{"ok":true,"time":"..."}`
- `http://localhost:4000/api/docs` → Swagger UI

## Paso 10: Init git + .gitignore

`.gitignore` mínimo:

```
node_modules/
dist/
.env
.env.local
*.log
.DS_Store
```

```bash
git init
git add .
git commit -m "chore: initial NestJS setup"
git branch -M main
git remote add origin https://github.com/<TU_USER>/lab-back.git
git push -u origin main
```

Listo. Después de esto seguís con **03-ENV.md** y luego **04-DB-SCHEMA.md**.
